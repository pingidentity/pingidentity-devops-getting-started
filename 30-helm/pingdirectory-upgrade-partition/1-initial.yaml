# Base deployment with two replicas.
# See the README in this directory for instructions on deploying the example upgrade.
pingdirectory:
  enabled: true
  # Start on 9.0.0.1
  image:
    tag: 9.0.0.1-latest
  container:
    replicaCount: 2
