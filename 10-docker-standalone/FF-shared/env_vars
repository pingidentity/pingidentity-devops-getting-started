# the name of the docker network where all containers can resolve each other
NETWORK_NAME=pingnet

# This is the local path where runtime data will be persisted for the containers
# all containers will create their own sub-directory underneath this common base
RT_ROOT=/tmp/Docker

#
#
GIT_REPO=https://github.com/pingidentity/pingidentity-server-profiles